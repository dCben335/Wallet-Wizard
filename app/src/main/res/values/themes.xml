<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WalletWizard" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:navigationBarColor">@color/primary_color</item>
        <item name="actionBarStyle">@color/primary_variant_color</item>
        <item name="colorPrimary">@color/primary_variant_color</item>
        <item name="colorPrimaryDark">@color/primary_variant_color</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
    
    <style name="LoadingScreenAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

</resources>
